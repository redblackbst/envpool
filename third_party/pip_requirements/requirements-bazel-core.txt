# Minimal pip deps for Bazel builds of EnvPool.
# Target: Python 3.12/3.13/3.14 (incl. free-threaded 3.14t), Linux x86_64.
#
# Keep this file *small* so `rules_python`'s pip.parse stays fast.

setuptools
wheel

numpy>=2.2

dm-env

gymnasium>=1.0

optree>=0.14.0
packaging

absl-py

pytest

# JAX 0.7.1+ ships Python 3.14 and 3.14t wheels.
jax[cpu]>=0.7.1

# Optional: GPU support for JAX via the CUDA 12 plugin.
# If you enable this, keep the versions in sync with your installed JAX.
# jax-cuda12-plugin>=0.8.2
